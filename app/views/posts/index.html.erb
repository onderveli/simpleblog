<h1><%= @title %></h1>
<% @posts.each do |post| %>
	<div class="well">
		<h3><%= post.title %></h3>
		<p><%= truncate(post.body.html_safe, :length =>120) %></p>
		<%= link_to "Read More", post_path(post), :class => 'btn btn-default' %>
	</div>
<% end %>